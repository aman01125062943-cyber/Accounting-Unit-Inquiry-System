# خطة التنفيذ - نظام الاضابير (014-archives-system)

## السياق التقني
سيتم تنفيذ نظام أرشفة مجمع يعتمد على "الدفعات" (Batches). بدلاً من نقل السجلات فرادى، يتم تجميعها في سجل واحد مجمع في الواجهة الرئيسية، مع الاحتفاظ بكافة التفاصيل في جدول تابع للبحث والاستعادة.

## مراجعة الدستور (Constitution Check)
- [x] اتباع خطوات Spec-Kit.
- [x] استخدام المهارات الاحترافية.
- [x] الالتزام بنطاق المشكلة.

## التغييرات المقترحة

### 1. قاعدة البيانات (Database)
- [MODIFY] [DatabaseService.cs](file:///c:/Users/esth633/Desktop/hk/Services/DatabaseService.cs)
  - إنشاء جدول `ArchiveBatches` (الملخصات).
  - إنشاء جدول `ArchiveDetails` (التفاصيل).

### 2. الباك إند (Backend)
- [NEW] [AdabirEndpoints.cs](file:///c:/Users/esth633/Desktop/hk/Endpoints/AdabirEndpoints.cs)
  - `GET /api/adabir`: قائمة الدفعات.
  - `GET /api/adabir/{id}/details`: تفاصيل الدفعة مع البحث.
  - `POST /api/adabir/archive`: تنفيذ الأرشفة المجمعة بناءً على التاريخ.
  - `GET /api/adabir/preview`: عرض إحصائية قبل الأرشفة.
  - `POST /api/adabir/restore/{id}`: استعادة الدفعة بالكامل.

### 3. الواجهة الأمامية (Frontend)
- [MODIFY] [index.html](file:///c:/Users/esth633/Desktop/hk/wwwroot/index.html)
  - تحديث قائمة التنقل الجانبية.
  - إضافة قوالب (Templates) لشاشة الاضابير ونافذة عرض التفاصيل.
- [MODIFY] [app.js](file:///c:/Users/esth633/Desktop/hk/wwwroot/js/app.js)
  - إضافة منطق فتح نافذة الأرشفة من صفحة الحوافز.
  - تنفيذ طلب المعاينة (Preview) قبل الأرشفة.
  - منطق عرض الدفعات وفتح تفاصيل الدفعة (Modal).
  - ميزة البحث داخل تفاصيل الدفعة.

## خطة التحقق (Verification Plan)

### الاختبارات المؤتمتة
- استخدام `testsprite` للتحقق من تكامل نقاط النهاية.
- اختبار الأداء عند أرشفة أكثر من 10,000 سجل.

### التحقق اليدوي
- فتح نافذة الأرشفة والتأكد من صحة "العدد" المعروض بعد اختيار التاريخ.
- التأكد من اختفاء السجلات من صفحة الحوافز بعد الأرشفة.
- البحث عن سجل محدد داخل نافذة "عرض التفاصيل" في صفحة الاضابير.
- تجربة الاستعادة والتأكد من عودة البيانات لجدولها الأصلي وحذف الدفعة.
